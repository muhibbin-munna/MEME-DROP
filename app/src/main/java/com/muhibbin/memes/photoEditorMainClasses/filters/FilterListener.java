package com.muhibbin.memes.photoEditorMainClasses.filters;

import com.muhibbin.memes.photoEditorSDK.PhotoFilter;

public interface FilterListener {
    void onFilterSelected(PhotoFilter photoFilter);
}